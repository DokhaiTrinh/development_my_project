import { makeStyles } from '@mui/styles';

const Styles = makeStyles((theme) => ({
  paper: {
    padding: 20,

    width: 450,
    margin: '90px auto',
    elevation: 10,
  },
  avatar: {
    backgroundColor: '#1ab675',
  },
}));
export default Styles;
